const wysokoscWidoku = window.innerHeight;
console.log(`Wysokość widoku wynosi ${wysokoscWidoku}px`);
const szerokoscWidoku = window.innerWidth;
console.log(`Szerokość widoku wynosi ${szerokoscWidoku}px`);

